extends layout
block content
    #container.col
        if info
            p game over
        else
            p.
                您的学习计划：#{course.name}
                #{study.chinese}.
            p
                input(type='text',placeholder='回答',id='answer',name='answer')
            p
                a(href="/course/study?pid=#{course.id}&wid=#{study.id}") 看参考答案
